Implement the next eligible pending task in .agent/TASKS.json.

Follow AGENTS.md exactly.
Read the relevant existing code before editing.
Work on one task only.
After implementation run ./scripts/verify-ios.sh.
If verification fails, diagnose and fix, with a maximum of three implementation/verification attempts.
Do not change signing, capabilities, Team, bundle identifiers, or project.pbxproj unless I explicitly approved it.
Do not launch the Simulator.

When verification passes, stop and report:
- task id
- files changed
- verification result
- exact manual Simulator checks I should perform
Do not commit yet.
